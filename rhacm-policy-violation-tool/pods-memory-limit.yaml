apiVersion: v1
kind: Pod
metadata:
  name: memory-demo
  namespace: memory-test
spec:
  containers:
  - name: memory-demo-ctr
    image: polinux/stress
    resources:
      limits:
        memory: "10000Gi"
      requests:
        memory: "10000Gi"
    command: ["stress"]
    args: ["--vm", "1", "--vm-bytes", "800M", "--vm-hang", "1"]

